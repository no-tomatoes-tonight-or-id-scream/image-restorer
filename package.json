{
  "dependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev site",
    "docs:build": "vitepress build site --outDir docs --base /image-restorer/",
    "docs:preview": "vitepress preview site"
  },
  "devDependencies": {
    "vitepress-sidebar": "^1.29.0"
  }
}
